<% if @exchange_item.errors.any? %>
  $.flash("<%= @exchange_item.errors.full_messages.to_sentence %>", true );
<% else %>
  $('#exchange_list_items').html("<%= escape_javascript(render :partial => 'orders/items/exchange_list_items', :locals =>{ :exchange_items => exchange_items}) %>");
  $('#autocomplete_exchange_product').val('').focus()
  $('#exchange_quantity').val('')
  $.flash("<%=  @exchange_item.product_name %> adicionado com sucesso.");
<%end%>
