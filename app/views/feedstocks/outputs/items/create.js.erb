<% if @item.persisted? %>
  $('#list_items').html("<%= escape_javascript(render :partial => 'feedstocks/outputs/items/list_items', :locals =>{ :items => items}) %>");
  <% if @item.negative? %>
    $('tr').addClass('error')
  <% end %>
<% end %>

$('#autocomplete_feedstock').focus().val('')
$('#quantity').val('')
$('#feedstock_id').val('')